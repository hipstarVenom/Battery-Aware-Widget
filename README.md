# Battery-Aware-Widget
The application is built around a finite state machine (FSM) that reacts only to battery state changes (HIGH, MEDIUM, LOW, CRITICAL, CHARGING). Each state is mapped to an animation through a data-driven skin system, allowing full customization without modifying core logic.

pip install -r requirements.txt
